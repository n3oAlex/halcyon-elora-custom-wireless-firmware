# Dongle-only settings (ZMK merges every matching config/*.conf; this one matches
# only the halcyon_elora_dongle shield).

# ZMK Studio is enabled via build.yaml. Without locking, Studio can change the
# keymap immediately, no &studio_unlock key press required.
CONFIG_ZMK_STUDIO_LOCKING=n
