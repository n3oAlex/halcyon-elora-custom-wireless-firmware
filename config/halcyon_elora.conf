# Shared by dongle, left and right builds (ZMK merges every matching config/*.conf).

# The keymap references &mkp and &msc (touchpad click keys, encoder scroll). The Cirque
# shields already imply pointing on the dongle and right half; this makes the left
# half build the same behaviors so the shared keymap compiles everywhere.
CONFIG_ZMK_POINTING=y
